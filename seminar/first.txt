1) разбор бинарного пойиска в отсортированном массиве
2)разбор асимптотики  бинарный поиск log(n) > log(k) где k индекс искомого элемента 
3) делаем не простым бинарным поиском а по степеням 
двойки скачим, а когда напарываемся на 
нужный промежуток принимаем обычный бинарный поиск  
4) работа с очередью описание ее алгоритма 
        *реализуем на списке 
        *масив в нем элементы лежат подряд - статический масив 
        *когда работаем с динамическим масисивом постоянно увеличиваем в два 
            раза память и копируем в новый массив (погуглить)
        *список отличается от масива тем что данные хнаняться не друг за другом 
1)арифметика указателей
2)области хранения данных куча стек 
3) проверка указателя работа с ними С
4)fork
5) очередь на двух стеках (реолизовать стек) 
    push(v) {st1.push(v);}
    pop(v) (if (!st2.empty())) {
        while (!)
    }